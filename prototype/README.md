# Prototype

Visual and interaction reference only — **not production code**.

- `nextrep-app.html` — single-file React 18 prototype (CDN React + Babel standalone, hardcoded mock data).
- `ios-frame.jsx` — iOS 26 device frame for previewing the prototype.

See `../nextrep-app_planning_schema.md` §10.3 for what to take from these and what to ignore.
